import 'package:flutter/material.dart';

import 'utils/drawer_menu.dart';

class HomeVieww extends StatelessWidget {
  const HomeVieww({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Appbar"),
      ),
    );
  }
}
