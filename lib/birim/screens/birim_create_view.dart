import 'package:flutter/material.dart';

class BirimCreateView extends StatelessWidget {
  const BirimCreateView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("AAAA"),
    );
  }
}
